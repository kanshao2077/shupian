# 薯片 MVP 设计与交互验收

## 最终结论

**Passed。** 网页端核心路径、3:4 卡片导出、系统亮色适配、移动端布局和本地小红书填充模拟均通过。未发现 P0、P1 或 P2 级视觉问题。

真实小红书后台仍需要在用户已登录的 Chrome 中做一次接受性测试；这是外部页面结构与文件控件策略的风险，不影响本地 MVP 的视觉验收。

## 对照输入

- 源参考：`public/qa/source-card.png`，1080 × 1531。
- 最终实现：`public/qa/implementation-card.png`，1080 × 1440，透明圆角 PNG。
- 同屏对照：`qa/comparison-board-final.png`。
- 短文源参考：`codex-clipboard-a9d579c1-67f4-42de-950c-fa1bc5d9df62.png` 与 `F8BB9850-E4CC-4261-BC9A-BBC4ADE55456.jpeg`。
- 短文实际实现：`qa/short-poster-mineral-actual.jpg` 与 `qa/short-poster-highlight-actual.jpg`。
- 短文同屏对照：`qa/short-poster-comparison.jpg`。
- 短句排版优化前后：`qa/audit-poster-before.jpg` 与 `qa/audit-poster-after.jpg`。
- 自定义背景状态：`qa/audit-custom-background-after.jpg`。
- 长文底部标识二选一：`qa/audit-long-footer-x-after.jpg` 与 `qa/audit-long-footer-options-after.jpg`。
- 界面亮色桌面：`qa/theme-light-desktop.png`，1280 × 720。
- 界面亮色手机：`qa/theme-light-mobile.png`，390 × 844。
- 小红书填充模拟：`qa/xhs-mock-success.png`。

## 视觉判断

- 保留了图一的作者区、正文留白、段落节奏、底部分隔线与标识位置。
- 按用户要求将画布重排为 3:4，没有拉伸源图；正文区域仍完整且没有越界。
- 作者头像、昵称和认证标识使用图二身份信息。
- 默认卡片改为暗色是有意变化；卡片背景仍可自定义，不受界面系统主题切换影响。
- 新 Logo 用三张扇形展开的卡片切片表达“把图文切成一片片笔记”，在 42 px 和扩展小图标尺寸下均可辨识。
- 界面亮色模式使用冷灰白背景、纯白面板和蓝灰边框；系统暗色模式保留原有近黑工作台。
- 工作台按博朗式功能主义做了减法：去除重复英文标签、装饰图标和冗余状态提示，收紧圆角与阴影，同时保留红色主操作作为唯一强强调。

## 视口与响应式

| 视口 | 结果 |
| --- | --- |
| 1280 × 720 | 三栏编辑、预览、分页缩略图完整；顶部操作按钮不换行 |
| 821 × 720 | 顶部改为两行，三个操作按钮保持可读 |
| 390 × 844 | 无页面横向溢出；页码条独立横向滚动；可一键跳到实时预览 |

## 核心交互

| 路径 | 验收结果 |
| --- | --- |
| 长文自动分页 | 80 段内容生成多页，末页切回单页不崩溃 |
| 手动分页 | `---` 分隔符能生成新页 |
| 图文混排 | 文字与图片在同一个编辑区按真实顺序显示；光标处直接粘贴图片会切开当前段落并插入 |
| 任意比例图片 | 默认按 16:9 居中裁切，可切回原图比例；图片可在段落间上下移动，实际图片边缘应用自定义圆角 |
| 复制图文 | 剪贴板同时包含 `text/plain`、`text/html` 和首张 `image/png` |
| 一键切图 | 生成 1080 × 1440 PNG，并打包为 ZIP |
| 小红书填充模拟 | 成功填入 2 张卡片、标题和正文；最终发布按钮不被点击 |
| 系统主题 | 当前系统亮色实际截图通过；暗色基础样式与导出卡片回归通过 |
| 长文 / 短文切换 | 模式来回切换后，原长文、短句和作者信息均保留 |
| 短文样式切换 | “材质大字”与“重点摘录”即时切换；文字、圆角和颜色可独立调整 |
| 短句安全排版 | 按实际字宽、显式换行、行距和字距连续计算字号；4 行材质大字与 8 行重点摘录实测无裁切 |
| 材质背景记忆 | 上传后自动居中裁成 3:4；刷新后仍保留；“恢复默认”可清除并回到内置背景 |
| 长文底部标识 | X 与自定义文字二选一，始终只在右下角显示一个；预览与导出画布使用同一组件 |

## 迭代记录

1. 初版在 1280 × 720 下预览卡片被裁切，原生滚动条过亮。
2. 将预览宽度限制为 `58vh`，统一深色滚动条，卡片完整进入可视区。
3. 修复 821 px 头部按钮挤压、手机页码条撑宽和双层 sticky 遮挡。
4. 修复复制/导出/发布按钮进度串位、分页收缩后的选中页越界、媒体间距和真实图片圆角。
5. 加入系统亮色模式、新「薯片」Logo，并重新完成桌面与手机验收。
6. 将剪贴板写入改为用户点击时立即启动，并同步保留纯文本兜底；复验得到三种剪贴板格式。
7. 图片改为逐张解码、限制单图文件大小和总像素，再缩至导出所需尺寸，降低批量上传的内存峰值。
8. 将偏黄的暖米白替换为冷灰白体系，并按“功能优先、秩序清晰、少而精”的方向精简界面装饰。
9. 将正文输入框和独立图片列表合并为块式图文编辑器；删除段落下拉与“放到光标段”，改为在任意光标位置直接粘贴、添加和移动图片。
10. 增加“短文海报”模式，使用两张真实参考图生成无文字背景；材质大字贴近原图的大宋体留白，重点摘录保留粉色高亮、红色标记和底部作者区。
11. 首轮重点摘录因固定高度裁切默认文字；改为按字数和显式换行自动收缩字号，并用两组参考图与真实实现同屏复验。
12. 重新校正材质大字与重点摘录的字距、行距、文字框位置和自动换行；改为按真实字体测量并连续缩放，修复字号断崖与滑杆显示值不一致。
13. 增加材质大字自定义背景、浏览器内记忆和恢复默认；长文底部标识支持 X 与自定义文字二选一。
14. 收紧两种短文封面的四周空白，扩大中央文字安全区；短句容量由 80 字 / 8 行提升为 120 字 / 10 行，并继续按实际字宽自动缩放。
15. 将默认 X 标识放大到 42 px 并上移 6 px，自定义文字仍保持原右下角基线。
16. 长文初始样式改为曜石背景、20 px 卡片圆角、24 px 图片圆角、47 px 正文字号、1.45 行高和 X 标识；打开仍先进入内容面板。
17. 长文正文安全区扩大到约 952 × 1000 px；静态导出为每个测量行（包括尾部空行）生成真实行盒，避免图片在 PNG 中上移；新增当前页 PNG 导出。

## 工程验证

- `npm run build`：通过。
- `npm run test:sites`：4 项通过，0 失败。
- 主界面和小红书模拟页：未发现运行时错误。
- 扩展压缩包：`release/shupian-xhs-assistant.zip`，压缩包完整性检查通过。

## 已知外部风险

- 小红书创作后台的类名和结构不是稳定 API；扩展采用多级语义选择器，命中不唯一时会停止。
- 浏览器对脚本注入文件控件的支持可能变化；真实页面若拒绝 `File + DataTransfer`，需要退化为用户只选一次图片，标题和正文仍自动填充。
- 扩展严格不点击“发布”或“定时发布”，最终发布永远由用户校对后完成。

final result: passed
